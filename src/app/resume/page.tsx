import { PortfolioShell } from "@/components/portfolio-shell";

export default function ResumePage() {
  return (
    <PortfolioShell pageKicker="" pageTitle="" pageSubtitle="">
      <section className="w-full pl-[var(--content-offset-desktop)] max-[1024px]:px-[var(--space-xs)]">
        <p className="font-[var(--font-body)] text-[length:var(--size-body)] leading-[var(--lh-body)]">
          im still in the middle of making one sry :(
        </p>
      </section>
    </PortfolioShell>
  );
}
